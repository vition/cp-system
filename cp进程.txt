1，改成员工管理                              处理
2，项目页面显示 ，价格只能输入数字           处理
3，IP删除                                    处理

4，改成 IP查询及编辑                         处理
5，改成IP名称                                处理
6，IP日截、CP方联系人（管理员以上可以看），联系方式（超级管理员可以看）

7，系统公告                                  处理

8，编辑反馈框                                处理
9，分类删除                                  处理
10，基本信息                                 处理
11，评论管理 ALTER TABLE `comment` ADD `reply` INT(10) NOT NULL COMMENT '回复本表的id' AFTER `pid`;

ALTER TABLE `projects` ADD `deadline` VARCHAR(50) NULL COMMENT '截止时间' AFTER `feedback`, ADD `contact` VARCHAR(50) NULL COMMENT 'CP方联系人' AFTER `deadline`, ADD `telephone` VARCHAR(50) NULL COMMENT '联系电话' AFTER `contact`;

message 消息表
ALTER TABLE `projects` ADD `publisher` VARCHAR(50) NOT NULL ;

12，翻页功能 进行中
13，消息功能 进行中
14，前端查询功能
15，前端标签功能